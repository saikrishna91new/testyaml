pipeline {
agent any
  stages {
    stage("hello") {
                steps {
                    sh 'echo "sai"'
                  script {
                    def name = "${params.NAME}"
                    def gender = "${params.GENDER}"
                    
                  }
            }


    }




}
